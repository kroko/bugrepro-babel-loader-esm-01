/* eslint-disable no-unused-vars */

'use strict';

import 'index.scss';
import React from 'react';
import ReactDOM from 'react-dom';

import {AppDummy} from 'React/Containers/AppDummy/AppDummy.jsx';

ReactDOM.render(
  <AppDummy/>,
  document.querySelector('.app')
);
